apiVersion: v1
kind: ConfigMap
metadata:
  name: hadoop-config
  namespace: flink
data:
  core-site.xml: |
    <?xml version="1.0"?>
    <configuration>
      <!-- Placeholder: actual key injected by deploy-app.yml workflow -->
      <property>
        <name>fs.azure.account.key.prodeus2ordersstorage.dfs.core.windows.net</name>
        <value>PLACEHOLDER</value>
      </property>
    </configuration>
